{
  "topicSlug": "model-deployment",
  "domainId": 2,
  "taskId": "2.2",
  "overview": {
    "whyItMatters": "Choosing the right deployment strategy determines your application's cost, latency, and reliability profile. The wrong choice means either overpaying for unused capacity or struggling with performance issues. Understanding when to use Lambda, Bedrock provisioned throughput, or SageMaker endpoints is a core exam skill.",
    "estimatedMinutes": 30
  },
  "concepts": [
    {
      "id": "deployment-patterns",
      "title": "Model Deployment Patterns",
      "content": "There is no universal deployment strategy that works for every situation. The right choice depends on several factors: your traffic patterns, latency requirements, cost constraints, and whether you're using managed models or custom ones you've trained yourself.\n\n```mermaid\nflowchart TD\n    A{Custom or<br/>fine-tuned model?} -->|Yes| B[SageMaker Endpoints]\n    A -->|No| C{Traffic<br/>predictable?}\n    C -->|Variable| D[Lambda + On-Demand]\n    C -->|Predictable| E{Need &lt;50ms<br/>latency?}\n    E -->|Yes| F[Provisioned Throughput]\n    E -->|No| G[On-Demand - cheaper]\n```\n\nThe simplest deployment pattern combines Lambda with Bedrock's on-demand pricing. Your Lambda functions call Bedrock's InvokeModel API, and you pay only for the tokens you actually process. This approach requires zero infrastructure management—no servers to maintain, no capacity to provision, no scaling policies to configure. It works well for applications with variable traffic, development and testing environments, or any situation where cold starts are acceptable. The trade-off is that you pay a premium per token compared to committed capacity options.\n\nBedrock Provisioned Throughput represents the next level of commitment. You pre-purchase model capacity by committing to a throughput level measured in model units. In exchange for this commitment, you get guaranteed capacity that's always available and lower per-token costs. This makes sense for production workloads with predictable, sustained traffic where you can calculate expected usage and commit accordingly. The key decision point is utilization: if you'll use the capacity consistently, provisioned throughput saves money; if your traffic is unpredictable, you'll pay for capacity that sits idle.\n\nSageMaker endpoints become necessary when you need capabilities that Bedrock doesn't provide. If you've fine-tuned your own model, you'll deploy it to SageMaker. If you need specific instance types—particular GPU configurations, memory sizes, or compute profiles—SageMaker gives you full control over the underlying hardware. SageMaker also lets you manage model versions and implement gradual rollouts. The operational overhead is higher because you're managing infrastructure rather than just calling an API, but you gain flexibility that managed services can't provide.\n\nContainer-based deployment using ECS or EKS offers maximum flexibility for specialized requirements. You can deploy custom model serving frameworks, use specific GPU configurations, or integrate with existing container orchestration infrastructure your organization already operates. This approach has the highest operational overhead—you're responsible for container images, cluster management, networking, and scaling—but provides complete control over every aspect of the deployment.",
      "keyPoints": [
        "Lambda + Bedrock: Simplest, pay-per-use, best for variable traffic",
        "Provisioned Throughput: Committed capacity, lower per-token cost, predictable workloads",
        "SageMaker: Custom models, full hardware control, fine-tuning support",
        "Container-based: Maximum flexibility, highest operational overhead"
      ],
      "examTip": "'Least operational overhead' + FM access -> Bedrock. 'Custom model' or 'fine-tuned' -> SageMaker."
    },
    {
      "id": "model-cascading",
      "title": "Model Cascading for Cost Optimization",
      "content": "Model cascading is one of the most powerful cost optimization patterns available for foundation model applications. The core insight is simple: not every query requires your most capable (and expensive) model. By routing simple queries to smaller, cheaper models and reserving larger models for queries that actually need sophisticated reasoning, you can dramatically reduce costs while maintaining quality.\n\nThe pattern works by first classifying incoming queries by complexity. This classification can use a small, fast model or simple rule-based logic—it doesn't need to be sophisticated. Simple queries route to small models like Claude Haiku or Amazon Titan Express. These models handle straightforward tasks at a fraction of the cost of larger models. When the small model's response indicates uncertainty or insufficient quality, the query escalates to a larger model like Claude Sonnet or Opus.\n\nThe economics work because query complexity follows a power law distribution. In most applications, 70-80% of queries are simple: frequently asked questions, straightforward lookups, basic generations. Small models handle these perfectly well at 10-20x lower cost than large models. Only the remaining 20-30%—queries requiring complex reasoning, nuanced understanding, or sophisticated generation—truly benefit from larger models.\n\nImplementing cascading with Step Functions provides clean orchestration. The first state classifies complexity. A choice state routes to the appropriate model based on classification. After the model responds, an evaluation state assesses response quality or confidence. If quality is insufficient, the workflow escalates to a larger model; otherwise, it returns the response. This structure makes the logic explicit and debuggable.\n\nConsider the cost implications with concrete numbers. Processing 100,000 queries per day at 500 tokens each, using only Sonnet might cost $150 per day or roughly $4,500 monthly. With cascading that routes 70% of queries to Haiku and 30% to Sonnet, daily costs drop to approximately $54—a 64% reduction that saves nearly $2,900 per month.\n\nHere's a Python implementation demonstrating the cascading pattern:\n\n```python\nimport boto3\nimport json\n\nclient = boto3.client('bedrock-runtime')\n\nasync def cascading_inference(query: str) -> dict:\n    haiku_response = invoke_model('anthropic.claude-3-haiku-20240307-v1:0', query)\n    \n    if is_confident_response(haiku_response):\n        return {'model': 'haiku', 'response': haiku_response, 'cost': 'low'}\n    \n    sonnet_response = invoke_model('anthropic.claude-3-sonnet-20240229-v1:0', query)\n    return {'model': 'sonnet', 'response': sonnet_response, 'cost': 'medium'}\n\ndef is_confident_response(response: str) -> bool:\n    uncertainty_phrases = ['I\\'m not sure', 'I don\\'t know', 'unclear', 'might be']\n    return not any(phrase in response.lower() for phrase in uncertainty_phrases)\n\ndef invoke_model(model_id: str, query: str) -> str:\n    response = client.invoke_model(\n        modelId=model_id,\n        body=json.dumps({\n            'anthropic_version': 'bedrock-2023-05-31',\n            'messages': [{'role': 'user', 'content': query}],\n            'max_tokens': 1024\n        })\n    )\n    return json.loads(response['body'].read())['content'][0]['text']\n```\n\nThe is_confident_response function demonstrates a simple heuristic for quality evaluation. Production implementations might use more sophisticated approaches: semantic analysis of the response, explicit confidence scores from models that provide them, or comparison against expected response patterns.",
      "keyPoints": [
        "Classify query complexity before model selection",
        "Simple queries to small models (Haiku, Titan Express)",
        "Complex queries escalate to large models (Sonnet, Opus)",
        "Can reduce costs 50-70% compared to single-model approach"
      ]
    },
    {
      "id": "container-optimization",
      "title": "Container-Based Deployment Optimization",
      "content": "When you deploy models in containers using ECS, EKS, or SageMaker, optimization becomes more hands-on. You're directly managing compute resources, which means understanding how large language models consume memory, utilize GPUs, and process tokens.\n\nMemory management is the first consideration because LLMs are exceptionally memory-hungry. A 7-billion parameter model requires approximately 14GB just for the model weights when using FP16 (16-bit floating point) precision. This doesn't include memory for activations during inference, batch processing overhead, or system requirements. Quantization techniques like INT8 or INT4 reduce memory requirements substantially—potentially halving or quartering the memory footprint—but come with some quality trade-offs that you should evaluate for your specific use case. Right-sizing containers requires balancing these factors: provision too little memory and you'll face out-of-memory errors; provision too much and you're wasting money on unused resources.\n\nGPU utilization presents its own optimization challenges. Machine learning inference workloads often show surprisingly low GPU utilization—30-40% is common—because of I/O bottlenecks, small batch sizes, and the sequential nature of token generation. Improving utilization requires batching requests so the GPU processes multiple inputs simultaneously, monitoring both GPU memory and compute utilization to understand where bottlenecks occur, and potentially serving multiple models on the same GPU to increase overall utilization.\n\nToken processing characteristics affect both latency and capacity planning. Input tokens process in parallel, which is fast, but output tokens generate sequentially—each new token depends on all previous tokens. This means longer outputs always take more time regardless of how powerful your hardware is. Streaming responses to users improves perceived latency even though total generation time remains the same, because users see text appearing rather than waiting for completion.\n\nScaling policies for LLM workloads should account for token throughput rather than just request counts. A handful of requests generating long outputs can saturate capacity that would otherwise handle hundreds of short-response requests. CloudWatch custom metrics tracking tokens per second and queue depth provide better scaling signals than simple request-count metrics.",
      "keyPoints": [
        "LLMs need substantial memory; quantization reduces requirements",
        "Batch requests to improve GPU utilization",
        "Output generation is sequential; streaming improves perceived latency",
        "Scale on token throughput, not just request count"
      ]
    },
    {
      "id": "inference-profiles",
      "title": "Cross-Region Inference with Inference Profiles",
      "content": "Inference profiles enable cross-region inference, automatically routing requests to healthy regions when your primary region experiences capacity constraints or outages. This improves availability without requiring you to build custom failover logic.\n\nAn inference profile is an ARN that references a model across multiple regions. Instead of calling a specific model in a specific region, you call the inference profile, and Bedrock routes your request to an available region automatically. The routing respects data residency—US-based profiles route within the US, EU-based profiles stay in EU regions.\n\n**Inference Profile ARN Format:**\n```\narn:aws:bedrock:us-east-1:123456789012:inference-profile/us.anthropic.claude-3-sonnet-20240229-v1:0\n```\n\nThe prefix (like `us.`) indicates the geographic scope. Requests using a US profile might route to us-east-1, us-west-2, or other US regions based on availability. This happens transparently—your code doesn't change based on which region ultimately serves the request.\n\n```python\nimport boto3\nimport json\n\nclient = boto3.client('bedrock-runtime')\n\n# Using inference profile instead of direct model ID\nresponse = client.invoke_model(\n    modelId='arn:aws:bedrock:us-east-1:123456789012:inference-profile/us.anthropic.claude-3-sonnet-20240229-v1:0',\n    body=json.dumps({\n        'anthropic_version': 'bedrock-2023-05-31',\n        'max_tokens': 1024,\n        'messages': [{'role': 'user', 'content': 'Hello'}]\n    })\n)\n```\n\n**When to use Inference Profiles:**\n- Production workloads requiring high availability\n- Applications that can't tolerate regional outages\n- Global applications where latency to a single region varies\n\n**When NOT to use:**\n- When data must stay in a specific region for compliance\n- Development/testing where consistent behavior matters more than availability\n- When using provisioned throughput (committed to specific region)\n\n**Key exam points:**\n- Inference profiles are NOT the same as provisioned throughput\n- Geographic prefixes control routing scope (us., eu., etc.)\n- Profiles work with on-demand pricing, not committed capacity",
      "keyPoints": [
        "Inference profiles enable automatic cross-region failover",
        "Geographic prefixes (us., eu.) control routing boundaries",
        "Use for high availability; requests route to healthy regions",
        "Works with on-demand pricing, not provisioned throughput"
      ],
      "examTip": "'Cross-region inference' or 'automatic failover' or 'high availability' → Inference Profiles"
    },
    {
      "id": "batch-inference",
      "title": "Batch Inference for Bulk Processing",
      "content": "Batch inference processes large volumes of requests asynchronously, trading latency for cost efficiency. Instead of paying on-demand prices for each request, batch jobs process inputs at roughly 50% discount—significant savings for bulk workloads.\n\n**When to use Batch Inference:**\n- Processing thousands of documents for summarization\n- Bulk data enrichment (sentiment analysis, classification)\n- Content generation at scale (product descriptions, translations)\n- Offline evaluation datasets\n- Any workload where real-time response isn't required\n\n**How Batch Inference Works:**\n\n1. Prepare input data in JSONL format in S3\n2. Create a batch inference job via `CreateModelInvocationJob`\n3. Bedrock processes inputs asynchronously\n4. Results appear in S3 when complete\n\n**Input JSONL Format:**\n```json\n{\"recordId\": \"1\", \"modelInput\": {\"anthropic_version\": \"bedrock-2023-05-31\", \"max_tokens\": 256, \"messages\": [{\"role\": \"user\", \"content\": \"Summarize this article: ...\"}]}}\n{\"recordId\": \"2\", \"modelInput\": {\"anthropic_version\": \"bedrock-2023-05-31\", \"max_tokens\": 256, \"messages\": [{\"role\": \"user\", \"content\": \"Summarize this article: ...\"}]}}\n```\n\nEach line is a complete JSON object with a unique `recordId` and `modelInput` matching the model's native format.\n\n**Creating a Batch Job:**\n```python\nimport boto3\n\nclient = boto3.client('bedrock')\n\nresponse = client.create_model_invocation_job(\n    jobName='my-batch-job',\n    modelId='anthropic.claude-3-haiku-20240307-v1:0',\n    roleArn='arn:aws:iam::123456789012:role/BedrockBatchRole',\n    inputDataConfig={\n        's3InputDataConfig': {\n            's3Uri': 's3://my-bucket/input/',\n            's3InputFormat': 'JSONL'\n        }\n    },\n    outputDataConfig={\n        's3OutputDataConfig': {\n            's3Uri': 's3://my-bucket/output/'\n        }\n    }\n)\n\njob_arn = response['jobArn']\n```\n\n**Output Format:**\nResults appear as JSONL in the output S3 location, with each line containing the `recordId` and model response.\n\n**Cost Comparison:**\n| Method | Claude 3 Haiku (per 1K input tokens) | Best For |\n|--------|--------------------------------------|----------|\n| On-demand | $0.00025 | Real-time, interactive |\n| Batch | ~$0.000125 (50% off) | Bulk processing |\n| Provisioned | Variable (committed) | Sustained high volume |\n\n**Monitoring Batch Jobs:**\n```python\n# Check job status\nstatus = client.get_model_invocation_job(jobIdentifier=job_arn)\nprint(status['status'])  # InProgress, Completed, Failed, etc.\n```",
      "keyPoints": [
        "Batch inference saves ~50% vs on-demand pricing",
        "Input: JSONL files in S3 with recordId + modelInput",
        "Use CreateModelInvocationJob API to submit jobs",
        "Best for bulk processing where latency isn't critical"
      ],
      "examTip": "'Bulk processing' or 'cost-effective large volume' or 'offline inference' → Batch Inference"
    }
  ],
  "skillExplanations": [
    {
      "skillId": "2.2.1",
      "explanation": "Deployment patterns should match infrastructure choices to workload characteristics. Lambda with on-demand Bedrock serves as an excellent starting point for most applications. You write a function, call the Bedrock API, and let AWS handle everything else. Scaling is automatic, there's no infrastructure to manage, and you pay only for what you use. The main limitation is cold start latency, which can add seconds to response time for functions that haven't been invoked recently.\n\nBedrock provisioned throughput addresses cold starts and capacity uncertainty by reserving dedicated model capacity. You calculate your expected throughput needs, commit to purchasing that capacity measured in model units, and receive consistent performance in return. The economics favor provisioned throughput when utilization exceeds roughly 40-60%—below that threshold, you're paying for capacity you don't use; above it, you're saving compared to on-demand pricing.\n\nSageMaker endpoints become necessary in specific situations: when you've fine-tuned a model that isn't available in Bedrock, when you need hardware configurations Bedrock doesn't offer, or when you require capabilities like A/B testing between model versions. Multi-model endpoints let you host multiple models on shared infrastructure, improving resource utilization when you have several models with moderate individual traffic.",
      "awsServices": [
        {
          "serviceName": "AWS Lambda",
          "whenToUse": "Serverless FM invocation for variable traffic, event-driven architectures.",
          "examKeywords": ["serverless", "on-demand", "event-driven", "variable traffic"]
        },
        {
          "serviceName": "Amazon Bedrock",
          "whenToUse": "Managed FM access. On-demand for variable traffic, provisioned for production.",
          "examKeywords": ["managed", "provisioned throughput", "FM access"]
        },
        {
          "serviceName": "Amazon SageMaker",
          "whenToUse": "Custom models, fine-tuned models, specific hardware requirements.",
          "examKeywords": ["custom model", "fine-tuned", "endpoint", "hardware control"]
        }
      ]
    },
    {
      "skillId": "2.2.2",
      "explanation": "Container-based deployment provides full control over the inference environment at the cost of increased operational responsibility. ECS and EKS let you deploy custom model serving frameworks, select specific instance types, and integrate with container infrastructure your organization already manages.\n\nMemory management requires careful attention because underprovisioning causes immediate failures while overprovisioning wastes budget. Profile your model's actual memory requirements under realistic inference conditions. Model weights are just the baseline; add headroom for activation memory during inference, batch processing overhead, and operating system requirements. A model that loads successfully might still fail under production batch sizes.\n\nGPU utilization optimization often yields significant efficiency gains because ML workloads frequently leave GPUs underutilized. Small batch sizes don't fully occupy GPU compute capacity. I/O operations between CPU and GPU memory create idle periods. Techniques to improve utilization include increasing batch sizes where latency permits, continuous batching that adds new requests to in-progress batches, multi-model serving that shares GPU resources across models, and workload consolidation that combines multiple inference jobs.",
      "awsServices": [
        {
          "serviceName": "Amazon ECS",
          "whenToUse": "Container orchestration for model serving with AWS-managed control plane.",
          "examKeywords": ["container", "ECS", "Fargate", "model serving"]
        },
        {
          "serviceName": "Amazon EKS",
          "whenToUse": "Kubernetes-based model serving for teams with K8s expertise.",
          "examKeywords": ["Kubernetes", "EKS", "container orchestration"]
        },
        {
          "serviceName": "AWS Fargate",
          "whenToUse": "Serverless containers when you don't need GPU or want simpler management.",
          "examKeywords": ["serverless container", "Fargate"]
        }
      ]
    },
    {
      "skillId": "2.2.3",
      "explanation": "Cost optimization in model deployment combines right-sizing infrastructure with intelligent request routing. Model cascading represents the highest-impact optimization for most applications because it fundamentally changes the cost structure by routing requests to the cheapest capable model.\n\nThe insight behind cascading is that model capabilities often exceed task requirements. A query asking \"What is the capital of France?\" doesn't need Claude Opus's sophisticated reasoning capabilities—Haiku handles it perfectly at a fraction of the cost. By implementing classification and routing logic, you reserve expensive model capacity for queries that benefit from it.\n\nSmaller models fine-tuned for specific domains sometimes outperform larger general-purpose models on their specialized tasks while costing dramatically less. If your application focuses on a specific domain—legal documents, medical questions, technical support—consider whether a fine-tuned smaller model might serve better than a general large model.\n\nAPI-based cascading implements tiered model usage through code. Start every request with the cheapest appropriate option. Evaluate the response for quality or confidence indicators. Escalate to more capable models only when the cheaper option proves insufficient. This requires implementing quality evaluation logic, but the cost savings often justify the engineering investment.",
      "awsServices": [
        {
          "serviceName": "Amazon Bedrock",
          "whenToUse": "Access multiple model sizes for cascading. Haiku for simple, Sonnet/Opus for complex.",
          "examKeywords": ["model selection", "cascading", "tiered", "cost optimization"]
        },
        {
          "serviceName": "Amazon SageMaker",
          "whenToUse": "Multi-model endpoints for efficient resource sharing across models.",
          "examKeywords": ["multi-model", "endpoint", "resource sharing"]
        }
      ]
    }
  ],
  "serviceComparisons": [
    {
      "title": "Model Deployment Options",
      "services": ["Lambda + Bedrock On-Demand", "Bedrock Provisioned Throughput", "SageMaker Endpoints"],
      "criteria": [
        {
          "criterion": "Operational overhead",
          "values": {
            "Lambda + Bedrock On-Demand": "Lowest - fully managed",
            "Bedrock Provisioned Throughput": "Low - managed with capacity commitment",
            "SageMaker Endpoints": "Medium - manage endpoints and scaling"
          }
        },
        {
          "criterion": "Cost model",
          "values": {
            "Lambda + Bedrock On-Demand": "Pay per token, no commitment",
            "Bedrock Provisioned Throughput": "Hourly commitment, lower per-token",
            "SageMaker Endpoints": "Per-instance-hour + data transfer"
          }
        },
        {
          "criterion": "Best for",
          "values": {
            "Lambda + Bedrock On-Demand": "Variable traffic, development, unpredictable usage",
            "Bedrock Provisioned Throughput": "Production with predictable, sustained traffic",
            "SageMaker Endpoints": "Custom models, fine-tuned models, specific hardware"
          }
        },
        {
          "criterion": "Model flexibility",
          "values": {
            "Lambda + Bedrock On-Demand": "Bedrock-available models only",
            "Bedrock Provisioned Throughput": "Bedrock-available models only",
            "SageMaker Endpoints": "Any model you can deploy"
          }
        }
      ]
    }
  ],
  "examStrategies": [
    {
      "questionPattern": "high availability or cross-region failover",
      "approach": "Inference Profiles for automatic cross-region routing. Not the same as provisioned throughput. Geographic prefixes (us., eu.) control routing boundaries.",
      "keywords": ["high availability", "cross-region", "failover", "automatic routing"]
    },
    {
      "questionPattern": "bulk processing or offline inference",
      "approach": "Batch Inference via CreateModelInvocationJob. ~50% cost savings vs on-demand. Input/output via JSONL in S3. Use for non-real-time workloads.",
      "keywords": ["bulk", "batch", "offline", "cost savings", "large volume"]
    },
    {
      "questionPattern": "minimize operational overhead for FM access",
      "approach": "Bedrock (on-demand or provisioned) over SageMaker. Lambda + Bedrock is the most hands-off option. SageMaker requires endpoint management.",
      "keywords": ["operational overhead", "managed", "least effort", "FM access"]
    },
    {
      "questionPattern": "custom or fine-tuned model deployment",
      "approach": "SageMaker endpoints are required for custom models. Bedrock only hosts its available models. If the question mentions fine-tuning a custom model, SageMaker is the answer.",
      "keywords": ["custom model", "fine-tuned", "deploy custom", "own model"]
    },
    {
      "questionPattern": "cost optimization with variable traffic",
      "approach": "Model cascading with on-demand pricing. Don't commit to provisioned throughput for variable workloads. Route simple queries to small models.",
      "keywords": ["cost", "variable traffic", "optimization", "unpredictable"]
    }
  ],
  "keyTakeaways": [
    "Lambda + Bedrock on-demand: simplest, pay-per-use, best for variable traffic",
    "Provisioned throughput: committed capacity for production workloads with predictable traffic",
    "SageMaker: required for custom/fine-tuned models or specific hardware needs",
    "Model cascading routes simple queries to cheap models, saving 50-70% on costs",
    "Inference profiles enable cross-region failover for high availability",
    "Batch inference saves ~50% for bulk processing via JSONL in S3",
    "Container deployments need careful memory and GPU optimization"
  ],
  "commonMistakes": [
    "Using provisioned throughput for variable traffic (overpaying for unused capacity)",
    "Using SageMaker when Bedrock would suffice (unnecessary operational overhead)",
    "Not implementing model cascading when query complexity varies",
    "Confusing inference profiles with provisioned throughput (different purposes)",
    "Using on-demand for bulk processing when batch inference saves 50%",
    "Under-provisioning container memory for LLM workloads",
    "Scaling on request count instead of token throughput"
  ]
}
